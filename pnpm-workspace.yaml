packages:
  - "apps/*"
  - "packages/*"

# pnpm 11 requires explicit allow-listing for packages with native postinstall
# scripts. Set to true for trusted packages; false to keep them blocked.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
